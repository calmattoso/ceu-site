var int v = 0;
par/or do
    loop do
        await 10ms;
        v = v + 1;  // nondet access (on 10th iteration)
    end
with
    await 100ms;
    v = v * 2;      // nondet access
end
return v;           // returns 19 or 20
