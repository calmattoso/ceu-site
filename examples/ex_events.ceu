// input event identifiers begin in uppercase
input int MyEvt;    // 'MyEvt' is an event of ints
loop do
    int v = await MyEvt; // v = next 'MyEvt'
    _printf("MyEvt=%d\n", v);
    if v == 0 then
        break;      // escapes the loop when v==0
    end
end
return 0;
