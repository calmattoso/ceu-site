var int ret;
var int* ptr = &ret;

async (ptr) do
    // calculates fat of 10
    var int fat = 1;
    var int i = 10;
    loop do
        if i == 1 then
            break;
        end
        fat = fat * i;
        i = i - 1;
    end
    *ptr = fat;
end;

return ret;
