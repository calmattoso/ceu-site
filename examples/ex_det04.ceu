input void A,B;
int v = 0;
par/or do
    par/and do
        await A;
    with
        await B;
    end
    v = 1;              // non-deterministic access
with
    await A;
    await B;
    v = 2;              // non-deterministic access
end
return v;               // returns 1 or 2
