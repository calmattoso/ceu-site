// internal event (variable) identifiers begin in lowercase
event int i = 0;
par do
    loop do
        await i;        // waits for changes
        _printf("Hello World: %d!\n", i);
    end
with
    loop do
        await 250ms;
        emit i(i+1);    // assigns a new value and triggers 'i'
    end
end
