// internal event (variable) ids begin in lowercase
event int i = 0;
C _printf();
par do
    loop do
        await i;        // waits for changes
        _printf("Hello World: %d!\n", i);
    end
with
    loop do
        await 250ms;
        emit i(i+1);    // assigns and triggers 'i'
    end
end
