// calculates fat of 10
int fat = 1;
int i = 10;
loop do             // a tight loop
    if fat == 0 then
        break;
    end
    fat = fat * i;
    i = i - 1;      // this path does not await or break
end
return fat;
